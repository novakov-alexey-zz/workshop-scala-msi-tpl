server {
  host = localhost
  port = 8080
}

storage {
  url = "jdbc:postgresql://localhost:/trips"
  driver = "org.postgresql.Driver"
  user = ""
  password = "trips"
  connectionTimeout = 30000
  maximumPoolSize = 1000
}