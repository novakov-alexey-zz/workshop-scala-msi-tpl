server {
  host = localhost
  port = 8080
}

storage {
  url = "jdbc:postgresql://localhost:5432/trips"
  driver = "org.postgresql.Driver"
  user = "trips"
  password = "trips"
  connectionTimeout = 3000
  maximumPoolSize = 100
}